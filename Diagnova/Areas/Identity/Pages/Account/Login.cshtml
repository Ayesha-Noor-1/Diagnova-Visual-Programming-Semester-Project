@page
@model Diagnova.Areas.Identity.Pages.Account.LoginModel
@{
    ViewData["Title"] = "Sign in";
    ViewData["BodyClass"] = "dn-auth";
}

<div class="dn-auth-shell mx-auto">
    <p class="text-center mb-3">
        <a class="small link-secondary text-decoration-none fw-semibold" asp-area="" asp-page="/Index">← Back to Diagnova</a>
    </p>
    <div class="dn-auth-card card border-0 shadow-sm">
        <div class="card-body p-4 p-md-5">
            <p class="small text-uppercase fw-semibold text-success mb-2" style="letter-spacing: 0.12em;">Welcome back</p>
            <h1 class="h3 fw-bold mb-1">Sign in to Diagnova</h1>
            <p class="text-muted small mb-4">Use your email and password to continue.</p>

            <div asp-validation-summary="ModelOnly" class="text-danger small mb-3" role="alert"></div>

            <form id="account" method="post">
                <div class="mb-3">
                    <label asp-for="Input.Email" class="form-label small fw-semibold"></label>
                    <input asp-for="Input.Email" class="form-control form-control-lg rounded-3" autocomplete="username" placeholder="you@example.com" />
                    <span asp-validation-for="Input.Email" class="text-danger small"></span>
                </div>
                <div class="mb-3">
                    <label asp-for="Input.Password" class="form-label small fw-semibold"></label>
                    <input asp-for="Input.Password" class="form-control form-control-lg rounded-3" autocomplete="current-password" />
                    <span asp-validation-for="Input.Password" class="text-danger small"></span>
                </div>
                <div class="form-check mb-4">
                    <input asp-for="Input.RememberMe" class="form-check-input" />
                    <label asp-for="Input.RememberMe" class="form-check-label small"></label>
                </div>
                <button type="submit" class="btn dn-btn-success w-100 rounded-pill py-2 fw-semibold">Sign in</button>
            </form>

            <hr class="my-4 text-muted opacity-25" />

            <p class="small text-center text-muted mb-0">
                New here?
                <a class="link-success text-decoration-none fw-semibold" asp-page="./Register" asp-route-returnUrl="@Model.ReturnUrl">Create an account</a>
            </p>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
